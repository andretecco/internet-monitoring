---
name: ansible-lint
on:
  push:
    branches:
      - main
jobs:
  ansible_lint:
    name: Ansible Lint
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Run ansible-lint
        uses: ansible/ansible-lint@v6

  discord_message:
    name: Discord Message
    runs-on: ubuntu-latest
    needs: ansible_lint
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Determine Status
        id: determine_status
        run: echo "::set-output name=status::${{ job.status }}"

#      - name: Notify Discord
#        uses: rjstone/discord-webhook-notify@v1
#        if: steps.determine_status.outputs.status == 'failure'
#        with:
#          severity: error
#          details: Failed!
#          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}

      - name: Success
        uses: rjstone/discord-webhook-notify@v1
        if: success()
        with:
            severity: info
            details: Succeeded!
            webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
        
      - name: Failure
        uses: rjstone/discord-webhook-notify@v1
        if: failure()
        with:
            severity: error
            details: Failed!
            webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
        
      - name: Cancelled
        uses: rjstone/discord-webhook-notify@v1
        if: cancelled()
        with:
            severity: warn
            details: Cancelled!
            webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}